import { VinayakShopValidators } from './vinayak-shop-validators';

describe('VinayakShopValidators', () => {
  it('should create an instance', () => {
    expect(new VinayakShopValidators()).toBeTruthy();
  });
});
